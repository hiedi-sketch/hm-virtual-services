{
  "name": "print-shop",
  "version": "1.0.0",
  "description": "3D print shop — catalog costing, filament and material inventory, production queue",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server/index.js",
    "dev": "echo \"Run 'npm run dev --prefix server' and 'npm run dev --prefix client' in two terminals\""
  }
}
