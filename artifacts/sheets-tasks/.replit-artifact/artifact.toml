kind = "web"
previewPath = "/sheets-tasks/"
title = "Sheet Task Manager"
version = "1.0.0"
id = "artifacts/sheets-tasks"
router = "path"

[[services]]
name = "web"
paths = ["/sheets-tasks/", "/sheets-tasks"]
localPort = 5000

[services.development]
run = "cd artifacts/sheets-tasks && python app.py"

[services.env]
PORT = "5000"
BASE_PATH = "/sheets-tasks/"
